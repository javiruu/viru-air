# 01 — Truth Reconciliation Matrix

**Fecha:** 2026-09-12  
**Commit auditado:** 91e6717d9bbd43d4ba148c6a114426266fabb823  

## Matriz de Reconciliacion

| Claim de Modernizacion | Evidencia Esperada | Evidencia Real Hallada | Estado | Accion |
|---|---|---|---|---|
| **155 endpoints vs 121 OpenAPI paths** | Relacion matematica y estructural exacta entre `app.routes` y `docs/architecture/openapi.json`. | `len(app.routes) == 155`. 4 rutas internas excluidas del schema (`/openapi.json`, `/docs`, `/docs/oauth2-redirect`, `/redoc`). 151 rutas de endpoints de app. 26 paths tienen multiples metodos registrados como rutas separadas (30 metodos adicionales). Total paths unicos en OpenAPI = 121. Total operaciones HTTP en OpenAPI = 151. | `VERIFIED_OK` | Explicado y documentado formalmente. No faltan rutas en el contrato OpenAPI. |
| **OpenAPI / Orval como Single Source of Truth** | Script exportador reproducible y regeneracion determinista. Adopcion real de clientes en el frontend. | `export_openapi.py` genera 121 paths identicos sin diff. `npm run api:check` pasa con codigo de salida 0. **Sin embargo**, `frontend/src/` contiene 0 imports de `src/api/generated/`. El producto sigue usando `fetch` nativo en el 100% de call sites. | `PARTIAL_SCAFFOLD` | Deferir adopcion en producto; mantener contrato y exportador automaticos, documentando que no hay call sites en frontend aun. |
| **TanStack Query adoptado** | Reduccion demostrada de useEffect, uso de useQuery y useMutation. | `QueryProvider` esta envuelto en `frontend/src/app/layout.tsx`. En `frontend/src/` hay **0 llamadas a useQuery**, **0 llamadas a useMutation** y **243 useEffect**. Ningun flujo de servidor fue migrado. | `PARTIAL_SCAFFOLD` | Mantener el provider configurado para nuevos desarrollos sin forzar migracion masiva destructiva de los 243 efectos preexistentes. |
| **Componentes shadcn/ui (Button, Badge, Card)** | Primitivas reutilizadas en pantallas del producto. | Existen archivos en `frontend/src/components/ui/{button,badge,card}.tsx`. En `frontend/src/` hay **0 imports** de estos componentes en pantallas del producto (solo importados en su propio unit test `shadcn-ui-primitives.test.tsx`). | `DEFERRED` | Mantener como primitivas preparadas segun directriz de `AGENTS.md` (no forzar migracion de UI existente sin peticion explicita). |
| **PostHog Analytics y Feature Flags** | Inicializacion en startup, tracking de 5 eventos en pantallas reales, gestion de identidad. | `posthog.ts` define configuracion y wrapper seguro. **No se inicializa en layout.tsx** ni en ninguna pagina/componente. Los 5 eventos definidos no se llaman en ningun call site del producto (solo en su unit test). | `PARTIAL_SCAFFOLD` | Mantener wrapper seguro; no forzar eventos hasta que haya instrumentacion real aprobada. |
| **OpenTelemetry Tracing** | Spans en endpoints, base de datos o startup. | `backend/app/core/telemetry.py` existe pero **no se importa en app/main.py** ni en ningun middleware/endpoint. Ningun span se emite en runtime (solo en unit test aislado). Sin exporter OTLP configurado. | `LOCAL_ONLY_SCAFFOLD` | Documentar honestamente como modulo no-op / scaffold sin fingir observabilidad distribuida. |
| **Zod / Runtime Boundaries** | Validacion de env y tokens de almacenamiento local. | `frontend/src/lib/env.ts` y `frontend/src/modules/shared/auth-schema.ts` implementan validacion Zod. No estaban conectados a los entrypoints reales de la aplicacion (solo a su test unitario). | `PARTIAL_SCAFFOLD` | Integrar validacion runtime en puntos de frontera donde proteja sin riesgo. |
| **Supabase Status: PARTIAL vs Completo** | Claridad en la autoridad de base de datos y alcance de Supabase. | Alembic tiene 65 versiones activas gestionando el 100% de tablas (incluyendo `flight_watch`). Supabase tiene 1 migracion SQL aislada en `supabase/migrations/` pero **0 conexiones de backend y 0 en frontend**. | `EXPERIMENTAL_ONLY` | Declarar Alembic como autoridad unica de base de datos. Supabase queda catalogado como experimento / scaffold desacoplado. |
| **Tests de Backend: 1.114 vs 5 especificos** | Ejecucion de suite completa de pruebas. | Pytest descubre y ejecuta **1.441 tests**. Se encontraron 8 fallos debidos a fechas hardcodeadas en el pasado (`2026-09-03`) en tests de persistencia comunitaria, ya corregidos con fechas dinamicas futuras. Resultado actual: **1.441 tests pasados**. | `FIXED / VERIFIED_OK` | Corregidos tests regresivos por fecha estatica expirada; suite completa ejecutada y verde. |
| **Biome Scope: 16 archivos vs repositorio real** | Lint y formato sobre el alcance real configurado. | La modernizacion anterior reporto Biome sobre 16 archivos pasando una lista manual. Al ejecutar `biome check` sobre el alcance real (`frontend/biome.json`), se analizan **852 archivos** y se detectan 1.021 discrepancias de formato/estilo. | `FALSE_POSITIVE_CLAIM` | Mantener ESLint (`next/core-web-vitals`) como linter canonico oficial del proyecto y acotar Biome a scope deliberado. |

## Conclusiones de la Fase 01
1. Las diferencias de endpoints (155 vs 121) y tests (1.441 vs 5) han quedado completamente demostradas con numeros exactos.
2. Tecnologias anadidas como Orval, PostHog, OpenTelemetry y shadcn estan instaladas y probadas unitariamente, pero NO estan conectadas a flujos reales del producto. Deben clasificarse de forma honesta como scaffolds / deferred para evitar falsos positivos de "modernizacion completa".
3. Alembic es la unica autoridad real de migraciones de base de datos.

