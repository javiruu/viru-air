/**
 * // Generated by Orval (v8) — DO NOT EDIT MANUALLY
 * // Spec: docs/architecture/openapi.json
 * // Re-generate with: npm run api:generate
 */
import type { DoorToDoorCorridorOutStatus } from './doorToDoorCorridorOutStatus';

/**
 * Public-facing summary of a verified or planned real-data corridor.
 *
 * Curated fields only — full operational `notes` (which mention feed URLs,
 * operator accounts, and partial-window warnings) are summarised into a
 * short `notes_preview` so we don't leak infrastructure details to clients.
 */
export interface DoorToDoorCorridorOut {
  id: string;
  name: string;
  region: string;
  origin_area: string;
  destination_airport: string;
  status: DoorToDoorCorridorOutStatus;
  verified_at?: string | null;
  notes_preview?: string;
  both_legs?: boolean;
  coverage_has_nearby_stops?: boolean;
  coverage_service_by_date?: boolean | 'limited_window' | 'limited';
  airport_search_terms?: string[];
}
