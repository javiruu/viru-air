/** Minimum sample size required before community pricing data becomes publicly visible. */
export const COMMUNITY_MIN_SAMPLE_SIZE = 3;

/** Minimum watcher count before showing the "N personas siguen esta ruta" signal. */
export const COMMUNITY_MIN_WATCHERS = 5;

/** Standard community data window in days. */
export const COMMUNITY_WINDOW_DAYS = 7;
